/**
 * Action의 종류를 선언한다.
 * 앞에 export를 붙이면 나중에 불러올 때,
 * import * as types from './ActionTypes'로 부를 수 있다.
 * 액션을 선언은 대문자로 한다.
 */

export const INCREMENT = 'INCREMENT';
export const DECREMENT = 'DECREMENT';
export const SET_COLOR = 'SET_COLOR';
